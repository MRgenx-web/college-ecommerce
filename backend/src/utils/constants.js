// Mirrors frontend/src/utils/constants.js — kept in sync manually since the
// two apps don't share a package.
const CATEGORIES = ['Mobiles', 'Laptops', 'Audio', 'Accessories', 'Gaming', 'Wearables'];

module.exports = { CATEGORIES };
