PORT=5000
NODE_ENV=development

# SQLite
DB_PATH=./src/database/ecommerce.db

# JWT
JWT_SECRET=replace_this_with_a_long_random_secret_in_production
JWT_EXPIRES_IN=7d

# Admin bootstrap credentials (used only for seeding the first admin account)
ADMIN_EMAIL=admin@techkart.in
ADMIN_PASSWORD=Admin@123

# CORS
CLIENT_URL=http://localhost:5173
